# Overnight Report -- 2026-04-13

## Summary

No new commits since the 2026-04-10 overnight report commit. The codebase remains static. Django 5.1.1 is still the top-priority issue: EOL since December 2025 with 7+ unpatched CVEs including the April 7 batch (CVE-2026-3902 ASGI header spoofing, CVE-2026-33034 Content-Length bypass). The Russian locale is fully translated; English locale has expected empty msgstr entries (source language). All 13 metadata_fields.yaml model mappings are consistent with models.py. Tests could not be executed due to sandbox restrictions.

## Yesterday's Changes

No commits in the last 24 hours (or last 72+ hours). Most recent commits:

```
0b36558 chore: overnight research report 2026-04-10  (2026-04-10)
07cccd6 docs: add comprehensive Phase 4 and MVP completion summaries  (2026-02-13)
6225da8 fix: resolve Python 3.10 syntax error in test_locale_compiler  (2026-02-13)
9607a13 feat: implement VCF database helper functions  (2026-02-13)
f9a7bfa feat: add SampleGroupCreateView for manual sample group creation  (2026-02-13)
```

## TODOs/FIXMEs Found

None. Both `app/` and `MetagapUserCode/` are clean of TODO, FIXME, and HACK markers.

## Test Status

**Could not run tests** -- sandbox restrictions prevented `python manage.py test` and `coverage run` execution. Manual run required:

```bash
cd ~/MetaGap/MetaGap
python -m coverage run --source=app,MetagapUserCode manage.py test
python -m coverage report --include="*/app/*" --fail-under=70
python -m coverage report --include="*/MetagapUserCode/*" --fail-under=75
```

CI thresholds: `app/` >= 70%, `MetagapUserCode/` >= 75%.

Test file inventory: ~20 test modules in `app/tests/` and ~25 in `MetagapUserCode/tests/`. All major source modules have corresponding test files:

| Source Module | Test Coverage File(s) |
|---------------|----------------------|
| `views.py` | `test_views_accounts.py`, `test_views_additional.py`, `test_views_import_export.py`, `test_views_sample_groups.py`, `test_views_sample_group_access.py`, `test_views_search_dashboard.py`, `test_import_data_view.py` |
| `forms.py` | `test_forms.py` |
| `filters.py` | `test_filters.py`, `test_filters_additional.py` |
| `tables.py` | `test_tables.py` |
| `signals.py` | `test_signals.py` |
| `services/vcf_importer.py` | `test_vcf_importer.py`, `test_import_helpers.py` |
| `services/vcf_metadata.py` | `test_vcf_metadata.py`, `test_import_helpers.py` |
| `services/vcf_database.py` | `test_vcf_database.py` |
| `services/vcf_file_utils.py` | `test_vcf_file_utils.py` |
| `utils/locale_compiler.py` | `test_locale_compiler.py` |
| `admin.py` | **No dedicated test file** -- `FormatAdmin.sample_identifier` untested |
| `mixins.py` | Tested indirectly through view tests |

## Untranslated Strings

| Locale File | Raw empty `msgstr ""` count | Truly untranslated | Notes |
|-------------|----------------------------|-------------------|-------|
| `app/locale/ru/LC_MESSAGES/django.po` | 54 | **0** | All are multi-line PO entries where `msgstr ""` is followed by continuation lines with actual Russian text |
| `app/locale/en/LC_MESSAGES/django.po` | 46 | **0 (expected)** | Source language; empty msgstr is standard PO behavior for en locale. Some entries use inverted multi-line format |
| `MetaGap/locale/ru/LC_MESSAGES/django.po` | 1 | **0** | Only the PO header has empty msgstr (standard) |
| `MetaGap/locale/en/LC_MESSAGES/django.po` | 0 | **0** | Clean |

**All locales are fully translated.** No action needed.

## metadata_fields.yaml Consistency

All 13 entries in `metadata_models` map to existing Django model classes in `models.py`:

| YAML key | Model class | Status |
|----------|-------------|--------|
| `reference_genome_build` | `ReferenceGenomeBuild` | OK |
| `genome_complexity` | `GenomeComplexity` | OK |
| `sample_origin` | `SampleOrigin` | OK |
| `material_type` | `MaterialType` | OK |
| `library_construction` | `LibraryConstruction` | OK |
| `illumina_seq` | `IlluminaSeq` | OK |
| `ont_seq` | `OntSeq` | OK |
| `pacbio_seq` | `PacBioSeq` | OK |
| `iontorrent_seq` | `IonTorrentSeq` | OK |
| `bioinfo_alignment` | `BioinfoAlignment` | OK |
| `bioinfo_variant_calling` | `BioinfoVariantCalling` | OK |
| `bioinfo_post_proc` | `BioinfoPostProc` | OK |
| `input_quality` | `InputQuality` | OK |

**Notes:**
- `platform_independent` appears in `metadata_section_map` but intentionally has no entry in `metadata_models`. Data for this section is stored in `SampleGroup.additional_metadata` (unmapped VCF header data).
- `sample_group` is handled as a special case, not via `metadata_models`.
- All field aliases in `metadata_field_aliases` correspond to actual model fields on their respective models. Verified: `SampleGroup` (10 fields), `InputQuality` (6 fields), `ReferenceGenomeBuild` (3 fields), `GenomeComplexity` (3 fields), `SampleOrigin` (4 fields), `MaterialType` (2 fields), `LibraryConstruction` (4 fields), `IlluminaSeq` (5 fields), `OntSeq` (5 fields), `PacBioSeq` (4 fields), `IonTorrentSeq` (6 fields), `BioinfoAlignment` (4 fields), `BioinfoVariantCalling` (5 fields), `BioinfoPostProc` (2 fields).

No mismatches found.

## Library Updates Available

| Package | Current | Latest | Security? |
|---------|---------|--------|-----------|
| **Django** | 5.1.1 | **6.0.4** | **YES -- 7+ CVEs, EOL branch** |
| django-allauth | 65.0.1 | **65.15.0** | SAML fix in 65.14.1 |
| gunicorn | 23.0.0 | **25.3.0** | No known advisories |
| pysam | 0.22.1 | **0.23.3** | No known advisories |
| django-filter | 24.3 | **25.2** | No known advisories |
| django-tables2 | 2.7.0 | **2.9.0** | No known advisories |
| whitenoise | 6.11.0 | **6.12.0** | No known advisories |
| vcfpy | 0.14.2 | 0.14.2 | Up to date |

### Django Security Detail (last 30 days)

**Django 5.1.x has been EOL since December 31, 2025. No more security patches.**

**April 7, 2026** (Django 6.0.4, 5.2.13, 4.2.30):
- **CVE-2026-3902**: ASGI header name normalization allows security-sensitive header spoofing via underscore/hyphen confusion
- **CVE-2026-33034**: ASGI requests with missing/understated Content-Length bypass `DATA_UPLOAD_MAX_MEMORY_SIZE`, enabling unbounded memory consumption
- CVE-2026-4277, CVE-2026-4292, CVE-2026-33033: Additional fixes (details unresolved)

**March 3, 2026** (Django 6.0.3, 5.2.12, 4.2.29):
- **CVE-2026-25673**: `URLField.to_python()` DoS via slow NFKC normalization on Windows
- **CVE-2026-25674**: Race condition in file-system storage/cache backends causing incorrect permissions

## Suggestions

1. **CRITICAL: Upgrade Django from 5.1.1 to at least 5.2.13 (LTS).** This has been the top finding for 4 consecutive overnight reports. Django 5.1 is EOL with 7+ unpatched CVEs. The 5.2 LTS line receives security updates through April 2028. Alternatively, target Django 6.0.4 (current latest). Review deprecation notes for `staticfiles.finders.find()` `all` argument and auth login fallbacks.

2. **Upgrade django-allauth from 65.0.1 to 65.15.0.** The project is 15 minor versions behind. Version 65.14.1 fixed a SAML-related security issue. Note: 65.14.0+ drops Python 3.8/3.9 support (no impact for this project which requires 3.11+).

3. **Run tests manually and record coverage.** Tests have not been executed in any of the last 4 overnight runs due to sandbox restrictions. Run: `cd ~/MetaGap/MetaGap && coverage run manage.py test && coverage report`

4. **Add `admin.py` test coverage.** The custom `FormatAdmin.sample_identifier` method remains the only source module without any test file. A basic admin smoke test (verify list view loads, custom method returns expected sample_id from payload) would close this gap.

5. **Address CI configuration gap.** The workflow triggers on `main` branch but the repository default branch appears to be `master`. Verify CI is actually running on pushes. Consider adding ruff linting and bandit security scanning as parallel CI jobs.
